# Git
.git
.gitignore

# Hot-reload / build artifacts (air writes ./tmp)
tmp/
*.out

# Docs & CI
*.md
.github/

# Docker & compose (not needed inside the build context)
Dockerfile
Dockerfile.dev
docker-compose*.yml

# Tests live in a separate module and are not needed to build the binary
tests/

# Editor / OS / Syncthing artifacts
.vscode/
.idea/
.DS_Store
*.sync-conflict-*
